# JavaWeb / Spring Boot 初始审计笔记

## 1. 项目概况

- 项目名称：
- 仓库路径：
- 启动类：
- 技术栈：
- Spring Boot 版本：
- Java 版本：
- 输出形态：
  - REST API
  - 服务端模板渲染
  - 混合型
- 主要业务模块：

---

## 2. 启动与配置入口

### 2.1 启动入口

- 启动类位置：
- 是否单体应用：
- 是否多模块：

### 2.2 配置文件

- 主配置文件：
- profile 配置文件：
- 是否存在环境变量覆盖：
- 是否存在启动参数覆盖：

### 2.3 高价值配置项

- `server.*`：
- `management.*`：
- `spring.datasource.*`：
- `spring.sql.init.*`：
- `spring.jpa.*`：
- `spring.web.resources.*`：
- `spring.servlet.multipart.*`：
- `logging.*`：
- 其他自定义安全相关配置：

---

## 3. Web 暴露面

### 3.1 Controller 分布

- 主要 Controller：
- 路由前缀：
- 管理/后台相关路由：
- 调试/测试相关路由：

### 3.2 输出入口

- 是否主要返回 JSON：
- 是否主要返回模板视图：
- 模板目录：
- 静态资源目录：

### 3.3 可疑暴露面

- `/actuator`：
- `/debug`：
- `/test`：
- `/env`：
- `/config`：
- `/health`：
- 其他：

---

## 4. 认证与授权链

### 4.1 认证框架

- Spring Security：
- Shiro：
- Session：
- JWT：
- 自定义 Filter / Interceptor：
- 未发现显式认证框架：

### 4.2 登录入口

- 登录接口/页面：
- 当前用户恢复位置：
- 会话/Token 存储方式：

### 4.3 授权判断

- 角色判断位置：
- 资源归属判断位置：
- 是否存在显式未鉴权模块：
- 是否存在仅前端控制但服务端未校验：

---

## 5. 持久层与数据访问模式

### 5.1 持久层类型

- Spring Data JPA：
- MyBatis：
- JDBC：
- 混合型：

### 5.2 核心数据访问代码位置

- Repository：
- Mapper：
- Entity：
- XML SQL：

### 5.3 数据访问特征

- 是否存在动态 SQL：
- 是否存在原生 SQL：
- 是否存在按 ID 直接访问资源：
- 是否存在 owner / tenant / status 等约束下沉：

---

## 6. 输出边界

### 6.1 响应类型

- JSON DTO：
- 直接返回实体：
- 模板渲染：
- 文件下载：

### 6.2 模板与前端渲染

- 模板引擎：
- 是否存在动态模板 / 动态表达式：
- 是否存在富文本输出：
- 是否存在前端危险渲染点：

### 6.3 敏感字段暴露风险

- 密码字段：
- Token / Key / Secret：
- 角色 / 内部状态：
- 内部路径 / 类名 / 配置片段：

---

## 7. 异常、调试与错误处理

### 7.1 异常处理链

- 是否有全局异常处理：
- 是否存在 `@ControllerAdvice / @RestControllerAdvice`：
- 是否直接返回 `exception.getMessage()`：
- 是否有错误页：

### 7.2 调试与运维暴露

- Actuator 暴露情况：
- 是否存在调试接口：
- 是否存在测试入口：
- 是否存在故意抛异常入口：

### 7.3 错误信息风险

- 是否可能暴露堆栈：
- 是否可能暴露 SQL / 配置 / 路径：
- 是否可能暴露框架和组件信息：

---

## 8. 中间件与外部依赖

### 8.1 数据库

- 类型：
- 连接配置来源：
- 是否支持多 profile 切换：

### 8.2 缓存

- Redis / JCache / 本地缓存：
- 是否启用缓存注解：
- 缓存键和值大致范围：

### 8.3 外部组件

- MQ：
- 对象存储：
- 搜索引擎：
- 外部 HTTP 服务：
- 第三方认证 / 回调：
- 规则 / 脚本 / 表达式引擎：

---

## 9. 文件、URL、表达式、对象恢复等高风险能力

- 文件上传：
- 文件下载：
- 文件读取/写入：
- 远程 URL 抓取 / SSRF：
- Open Redirect：
- 命令执行相关调用：
- 表达式执行相关调用：
- 反序列化相关入口：
- 模板二次渲染：
- 其他高风险能力：

---

## 10. 第一轮高敏感点

---

## 11. 第二轮深入计划

---

## 12. 初步结论

- 项目类型：
- 主要暴露面：
- 主要身份链：
- 主要数据访问模式：
- 主要输出边界：
- 当前最值得优先深入的区域：
